# Dependency Policy

- Use Node 22.x and npm 10.x (see `.nvmrc` and `package.json` engines).
- Pin dependency versions exactly (no caret or tilde ranges).
- Update dependencies only after validating the HUD UI and recording changes in `CHANGELOG.md`.
